// Package version holds the application version reported to remote services.
//
// build/config.yml is the version of record; keep this constant in step with
// it when bumping (nothing enforces that today).
package version

const Version = "0.1.0"
